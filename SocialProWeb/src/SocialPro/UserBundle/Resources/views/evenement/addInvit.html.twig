{% extends "SocialProUserBundle::master.html.twig" %}
{% block content %}
    <div class="container">
    <div class="row">
    <!-- Contact form Section Start -->
    <div class="col-md-6">
        <h2>Inviter des gens à votre Evenement</h2>
        <form  method="post" class="contact">
            <div class="form-group">


                <font color="red" SIZE="4">{{ msg }}</font></a>


                {{ form_widget(form.idRecepteur, {'attr': {'placeholder': 'Nommer votre evénement','class': 'form-control input-lg'}}) }}


            </div>
            {{ form_widget(form.Inviter, { 'attr': {'class': 'btn btn-success'} }) }}
            <a href="{{ path('validerInvit') }}" ><input type="button"  name=" Terminez "value="Terminez" class="btn btn-responsive button-alignment btn-success" /></a>

            {{ form_end(form) }}
        </form>
    </div>
    <!-- //Conatc Form Section End -->
{% endblock %}