{% extends "SocialProUserBundle::master.html.twig" %}

{% block content %}
    <script src="http://ajax.aspnetcdn.com/ajax/jQuery/jquery-2.1.3.min.js"></script>
    <script src="{{ asset('js/rating.js') }}"></script>

    <div class="col-md-8">

        <h2 class="text-primary">L'evenement : {{ evenement.nom }} </h2>
        <br>

            <table width="100%" border ="1" cellspacing="1" cellpadding="1"><tr><td><div align=center>Description de l'evenement : &nbsp; <br> {{ evenement.description }}</div></td><tr></table>

        <br>
        <h5>{{ nb }} Personne(s) y participe</h5>
        <br>
        {% if nevv<1 %}
        <div>
        <h5>{{ nn }} Review(s) | Ajouter votre Review</h5>
        {{ form(form, {'attr': {'novalidate': 'novalidate'}}) }}
            {{ form_end(form) }}
        </div>
        {% else %}
            <h5>Vous avez attribuer la note {{ evall }} /5 à cette evenement</h5>

        {% endif %}


    </div>

{% endblock %}